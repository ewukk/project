{% include 'default/_form.html.twig' with {
    page_title: 'Edit a post'|trans({'%id%': post.id|default('')}),
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'post_index',
} %}