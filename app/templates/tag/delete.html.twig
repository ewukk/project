{% include 'default/_form.html.twig' with {
    page_title: 'Delete a tag'|trans({'%id%': tag.id|default('')}),
    form: form,
    submit_label: 'delete'|trans,
    back_to_list_path: 'tag_index',
} %}
